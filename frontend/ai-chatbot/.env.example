# Example environment for the Vercel AI Chatbot frontend.
# Copy to `frontend/ai-chatbot/.env.local` and adjust before running locally.
# Never commit secrets in this file; keep real keys in untracked overrides.

# Next.js dev port (optional override for `pnpm dev`).
PORT=3000

# Public runtime configuration used by the browser bundle.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# MCP backend integration — consumed by the server components and API routes.
MCP_API_BASE=http://localhost:8000
MCP_API_TOKEN=dev-token
MCP_SESSION_USER=regular

# Default provider hints for the finance/search artefacts.
LLM_PROVIDER=openai
LLM_MODEL=gpt-5-nano

# SearxNG base URL (only needed when the frontend calls it directly in dev).
SEARXNG_BASE_URL=http://localhost:8080

# Analytics / observability placeholders (optional).
ANALYTICS_WRITE_KEY=changeme

# ⚠️ Server-side secret placeholders — configure in `.env.local` for real runs.
# Leave OPENAI_API_KEY undefined during Playwright E2E runs to force the mock provider.
# OPENAI_API_KEY=sk-<your-openai-key-here>

# Playwright E2E helpers (uncomment/export when running browser tests locally).
# PLAYWRIGHT=1
# PLAYWRIGHT_TEST_BASE_URL=http://127.0.0.1:3000
