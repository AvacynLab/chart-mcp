# Example frontend environment variables for frontend/ai-chatbot
# Copy to frontend/ai-chatbot/.env.local for local development. Do NOT commit secrets.

# Next.js dev port (optional)
PORT=3000

# Public runtime config
NEXT_PUBLIC_API_BASE=http://localhost:8000

# AI and external services
OPENAI_API_KEY=sk-xxxx

# Analytics / instrumentation keys (optional)
ANALYTICS_WRITE_KEY=changeme

# SearxNG base URL used by frontend (if calling directly)
SEARXNG_BASE_URL=http://localhost:8080
# Example environment configuration for the Vercel AI Chatbot frontend.
# Copy this file to `.env.local` and adjust values to match your local backend.
MCP_API_BASE=http://localhost:8000
MCP_API_TOKEN=dev-token
MCP_SESSION_USER=regular
LLM_PROVIDER=openai
LLM_MODEL=gpt-5-nano
# Server-side provider key (do NOT expose on the client). Copy your real key into
# a non-committed file such as `.env.local` at the frontend root.
OPENAI_API_KEY=sk-<your-openai-key-here>
