# Environment template for chart-mcp.
# Every key listed here is documented in README.md; copy to `.env` and adjust locally.

# Token used to secure the FastAPI endpoints.
API_TOKEN=change-me
# Preferred market data exchange identifier.
EXCHANGE=binance
# Comma-separated list of origins allowed by CORS.
ALLOWED_ORIGINS=http://localhost:3000
# LLM provider/model pair leveraged by the analysis pipeline.
LLM_PROVIDER=stub
LLM_MODEL=heuristic-v1
# Interval (ms) between keep-alive events on SSE streams.
STREAM_HEARTBEAT_MS=5000
# Log verbosity for the application logger.
LOG_LEVEL=INFO
# Maximum number of authenticated requests per minute in production.
RATE_LIMIT_PER_MINUTE=60
# Toggle deterministic safeguards in tests (bypasses rate limiting when true).
PLAYWRIGHT=false
# Feature flag controlling exposure of finance-specific endpoints.
FEATURE_FINANCE=true

# Third-party credentials (leave blank if not using external providers yet).
OPENAI_API_KEY=
OPENAI_MODEL_ID=gpt-4o-mini
MARKET_DATA_API_KEY=
NEWS_API_KEY=
POSTGRES_URL=postgresql://user:password@localhost:5432/chart_mcp
